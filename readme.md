
# Tips for Data Analysis Report
## 1. Introduction
In this section, provide a brief overview of the data analysis project. Include the purpose of the analysis, the data sources used, and the key questions you aim to answer.

## installation by creating a virtual environment
```bash
python -m venv env
source env/bin/activate  # On Windows, use `env\Scripts\activate`
pip install -r requirements.txt
```
# activate the virtual environment in windows using git bash

```bash
source env/Scripts/activate
```
# install required packages from requirements.txt
```bash
# create a requirements.txt and add libraries to it
# install required packages from requirements.txt
pip install -r requirements.txt
```
## 2. create a ipynb file
```bash
# create a ipynb file

```bash
jupyter notebook
```
# create a ipynb file
mkdir scripts
cd scripts
tuch 01_amazon_eda.ipynb
  